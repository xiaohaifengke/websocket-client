import path from 'path'

export const projectRoot = path.resolve(__dirname, '../../')
export const distPath = path.resolve(projectRoot, 'dist')
export const srcPath = path.resolve(projectRoot, 'src')
export const pkgDistPath = path.resolve(distPath, 'dist')
